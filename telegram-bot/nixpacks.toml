# Railway (Nixpacks) build config
# ffmpeg দরকার HLS/M3U8 remux-এর জন্য।
[phases.setup]
nixPkgs = ["python311", "ffmpeg-full"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python3 bot.py"
